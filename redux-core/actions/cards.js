import types from './types';
import * as api from 'api/api-storage';

/*
 const {card} = types;

 export const addCard = newCard => {
 return {
 type: card.ADD,
 payload: api.addCard(newCard), // {deck with new card}
 };
 };

 export const removeCard = (deckId, cardId) => {
 return {
 type: card.REMOVE,
 payload: api.removeCard(deckId, cardId), // {deck without card}
 };
 };

 export const editCard = editedCard => {
 return {
 type: card.EDIT,
 payload: api.editCard(editedCard), // {edited Deck}
 };
 };*/
